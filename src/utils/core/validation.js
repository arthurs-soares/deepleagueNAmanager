// Re-export validation utilities from the organized modules
// This file maintains backward compatibility while the actual
// validation logic is now split into focused modules

module.exports = require('../validation');
